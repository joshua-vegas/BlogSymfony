<ul class="nav nav-pills nav-stacked">
  {% for article in listArticles %}
    <li>
      <a href="{{ path('jv_blog_view', {'id': article.id}) }}">
        {{ article.title }}
      </a>
    </li>
  {% endfor %}
</ul>
